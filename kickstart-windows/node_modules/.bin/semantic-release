#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../semantic-release/bin/semantic-release.js" "$@"
  ret=$?
else 
  node  "$basedir/../semantic-release/bin/semantic-release.js" "$@"
  ret=$?
fi
exit $ret
